# Render Static Site build config
# Allow legacy peer dependencies to avoid build failures
legacy-peer-deps=true

# Disable optional dependencies to speed up install
optional=false

# Use exact lockfile matching
package-lock=true
